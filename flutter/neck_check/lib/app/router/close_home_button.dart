import 'package:flutter/material.dart';

class CameraCloseButton extends StatelessWidget {
  const CameraCloseButton({super.key});

  @override
  Widget build(BuildContext context) {
    return CloseButton(color: Colors.white);
  }
}
